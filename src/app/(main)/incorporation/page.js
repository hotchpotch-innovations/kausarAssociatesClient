import Incorporation from '@/components/homepage/incorporation/Incorporation';
import React from 'react';

const page = () => {
    return (
        <section>
            <Incorporation isBg={false}/>
        </section>
    );
};

export default page;