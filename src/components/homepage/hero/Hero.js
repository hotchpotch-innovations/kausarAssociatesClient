import Slider from "./Slider";

const Hero = () => {
  return (
    <section>
        <div className="relative z-[-2] lg:z-[0] w-full h-[40vh] md:h-[60vh] lg:h-[90vh]">
          <Slider/>
        </div>

    </section>
  );
};

export default Hero;
